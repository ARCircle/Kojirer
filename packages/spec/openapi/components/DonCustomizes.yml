DonCustomizes:
  type: object
  properties:
    donId:
      type: string
      format: uuid
    toppingId:
      type: string
      format: uuid
    amount:
      type: integer
  required:
    - donId
    - toppingId
    - amount
