CustomizesPath:
  get:
    tags:
      - customize
    description:
    responses:
      '200':
        description: すべてのCustomizeを返す
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '../components/Customizes.yml#/Customizes'
