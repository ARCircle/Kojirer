{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path | select(endswith(\".js\") or endswith(\".ts\") or endswith(\".jsx\") or endswith(\".tsx\") or endswith(\".css\") or endswith(\".html\") or endswith(\".mjs\") or endswith(\".yml\") or endswith(\".yaml\") or endswith(\".json\") or endswith(\".md\"))' | xargs -r prettier --write"
          }
        ]
      }
    ]
  }
}
